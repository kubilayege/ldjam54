using UnityEngine;

[CreateAssetMenu(menuName = "Create DifficultySettings", fileName = "DifficultySettings", order = 0)]
public class DifficultySettings : ScriptableObject
{
    // TODO: Enemy Stats, spawn rates, Damage Numbers
}